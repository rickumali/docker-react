sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t rickumali/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true rickumali/docker-react npm run test
